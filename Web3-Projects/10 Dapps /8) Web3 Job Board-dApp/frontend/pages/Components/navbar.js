import React, { useState } from "react";
import Head from "next/head";
import { FiSun } from "react-icons/fi";
import { HiOutlineMoon } from "react-icons/hi";
import { ConnectButton } from "@rainbow-me/rainbowkit";
import Link from "next/link";
import { useGlobalContext } from "../../Context/Context";

const Navbar = () => {
  const { darkMode, toggleDarkMode } = useGlobalContext();

  return (
    <nav className="flex items-center justify-evenly p-4 bg-[#F2F4FB] dark:bg-[#1B2430] dark:text-white border-b-4 border-blue-300">
      <Head>
        <title>Job Board</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
        <style>
          @import
          url('https://fonts.googleapis.com/css2?family=Poppins:wght@500;600&display=swap');
        </style>
      </Head>
      <Link href="/">
        <h1 className="text-xl cursor-pointer">Home</h1>
      </Link>
      <Link href="/AvailableJobs">
        <h1 className="text-xl cursor-pointer">Jobs</h1>
      </Link>
      {darkMode ? (
        <HiOutlineMoon
          className="text-2xl cursor-pointer"
          onClick={toggleDarkMode}
        />
      ) : (
        <FiSun className="text-2xl cursor-pointer" onClick={toggleDarkMode} />
      )}
      
      <ConnectButton />
    </nav>
  );
};

export default Navbar;
